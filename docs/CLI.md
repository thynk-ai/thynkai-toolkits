# CLI

The CLI lives in `packages/cli`.

## Commands

- `thynkai init model` — scaffold a model entry directory and files
- `thynkai validate models` — validate a models registry tree
- `thynkai digest` — compute sha256 digest for a local file
- `thynkai publish` — dry-run summary of changes intended for a PR

Run `thynkai --help` for flags.

